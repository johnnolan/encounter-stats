export enum ChatMessageType {
  Damage = "damage",
  Attack = "attack",
  ItemCard = "itemCard",
  MidiQol = "midiqol",
  None = "none",
}

export enum RoleType {
  Fumble = "fumble",
  Critial = "critical",
}

export enum CombatantType {
  Character = "character",
  NPC = "npc",
}
