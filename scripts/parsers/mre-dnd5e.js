import {
  IsValidAttack,
  nullChecks,
  GetItemData,
  GetCombatantStats,
  CombatantStats,
  _add,
} from "../Utils.js";

export default async function MREDnd5e(stat, attackData, workflow) {
  return stat;
}
